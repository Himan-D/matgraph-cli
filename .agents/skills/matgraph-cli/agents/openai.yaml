interface:
  display_name: "Matgraph Cli"
  short_description: "Repo-specific patterns and workflows for matgraph-cli"
  default_prompt: "Use the matgraph-cli repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true